#!/bin/sh

OPTIMIZE='-O'
if [ "$LOCKDOWN_DEBUG" == "1" ]; then
    OPTIMIZE='-m cProfile -s time -o /tmp/lockdown.profile'
fi

PYTHONPATH=<BASE>/lockdown/lib python $OPTIMIZE <BASE>/lockdown/bin/lockdown -b <BASE>/lockdown "$@"
